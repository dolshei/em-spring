<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="site.dolshei.emspring.mvc.repository.BoardRepository">
    <select id="getList" parameterType="site.dolshei.emspring.mvc.domain.Board" resultType="site.dolshei.emspring.mvc.domain.Board">
        SELECT
            B.board_seq,
            B.title,
            B.contents,
            B.regdate
        FROM    board B
        ORDER BY B.regdate DESC
    </select>
</mapper>